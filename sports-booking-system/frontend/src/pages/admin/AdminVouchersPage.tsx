import { useState } from "react";import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { toast } from "sonner";import { adminApi } from "../../features/admin/api/adminApi";import { Button } from "../../components/ui/Button";import { Input } from "../../components/ui/Input";import { Select } from "../../components/ui/Select";import { ErrorState,LoadingState } from "../../components/common/States";import { AdminReasonModal } from "./AdminReasonModal";import { Table,THead,TBody,Tr,Th,Td } from "../../components/common/Table";
export function AdminVouchersPage(){const qc=useQueryClient();const[f,setF]=useState({search:"",status:""});const[pending,setPending]=useState<{id:string;action:"activate"|"disable"}|null>(null);const q=useQuery({queryKey:["admin-vouchers",f],queryFn:()=>adminApi.vouchers(f)});const action=useMutation({mutationFn:({id,action,reason}:{id:string;action:"activate"|"disable";reason:string})=>adminApi.setVoucherStatus(id,action,reason),onSuccess:async()=>{toast.success("Đã cập nhật voucher");setPending(null);await qc.invalidateQueries({queryKey:["admin-vouchers"]})},onError:e=>toast.error(e.message)});if(q.isLoading)return <LoadingState/>;if(q.isError)return <ErrorState message={q.error.message}/>;return <div className="space-y-5"><h1 className="text-3xl font-bold">Voucher toàn hệ thống</h1><div className="grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-2"><Input label="Tìm mã, tiêu đề, đối tác" value={f.search} onChange={e=>setF({...f,search:e.target.value})}/><Select label="Trạng thái" value={f.status} onChange={e=>setF({...f,status:e.target.value})} options={[{value:"",label:"Tất cả"},...["DRAFT","ACTIVE","EXPIRED","DISABLED"].map(x=>({value:x,label:x}))]}/></div><Table minWidth="900px"><THead><tr><Th>Mã</Th><Th>Voucher</Th><Th>Đối tác</Th><Th>Đã dùng</Th><Th>Trạng thái</Th><Th></Th></tr></THead><TBody>{q.data?.items.map(v=><Tr key={v.id}><Td className="font-bold">{v.code}</Td><Td>{v.title}</Td><Td>{v.businessName}<br/><span className="text-xs">{v.courtName||"Tất cả sân"}</span></Td><Td>{v.usedCount}/{v.usageLimit??"∞"}</Td><Td>{v.status}</Td><Td><Button variant={v.status==="ACTIVE"?"danger":"secondary"} onClick={()=>setPending({id:v.id,action:v.status==="ACTIVE"?"disable":"activate"})}>{v.status==="ACTIVE"?"Vô hiệu hóa":"Kích hoạt"}</Button></Td></Tr>)}</TBody></Table><AdminReasonModal open={Boolean(pending)} title="Cập nhật trạng thái voucher" onClose={()=>setPending(null)} onConfirm={reason=>pending&&action.mutate({...pending,reason})}/></div>}
