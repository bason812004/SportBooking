import { useQuery } from "@tanstack/react-query";import { Bar,BarChart,CartesianGrid,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";import { adminApi } from "../../features/admin/api/adminApi";import { LoadingState,ErrorState } from "../../components/common/States";import { PageHero } from "../../components/common/PageHero";
export function AdminStatisticsPage(){const q=useQuery({queryKey:["admin-statistics"],queryFn:adminApi.statistics});if(q.isLoading)return <LoadingState/>;if(q.isError)return <ErrorState message={q.error.message}/>;const d=q.data as any;const bookings=d.bookings.map((x:any)=>({status:x.bookingStatus,count:x._count,amount:x._sum.totalPrice}));return <div className="space-y-6"><PageHero eyebrow="Báo cáo" title="Thống kê hệ thống" subtitle="Tổng quan số liệu booking, sân và đánh giá trên toàn hệ thống." /><div className="grid gap-6 lg:grid-cols-2"><section className="h-96 rounded-2xl border bg-white p-5"><h2 className="font-bold">Booking theo trạng thái</h2><ResponsiveContainer width="100%" height="90%"><BarChart data={bookings}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="status"/><YAxis/><Tooltip/><Bar dataKey="count" fill="#2563eb"/></BarChart></ResponsiveContainer></section><section className="h-96 rounded-2xl border bg-white p-5"><h2 className="font-bold">Sân theo thành phố</h2><ResponsiveContainer width="100%" height="90%"><PieChart><Pie data={d.courtsByCity} dataKey="_count" nameKey="city" fill="#16a34a" label/><Tooltip/></PieChart></ResponsiveContainer></section></div><div className="grid gap-4 md:grid-cols-2"><div className="rounded-2xl border bg-white p-5"><p>Điểm đánh giá trung bình</p><p className="text-3xl font-bold">{Number(d.reviews._avg.rating??0).toFixed(1)}/5</p></div><div className="rounded-2xl border bg-white p-5"><p>Tổng đánh giá</p><p className="text-3xl font-bold">{d.reviews._count}</p></div></div></div>}
